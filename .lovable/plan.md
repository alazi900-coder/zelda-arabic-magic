

# اضافة فئة "القائمة الرئيسية" وتمييز نصوص شاشة العنوان

## المشكلة
نصوص القائمة الرئيسية (New Game, Continue, Settings, Options...) مدفونة ضمن فئة "القوائم والواجهة" الكبيرة التي تحتوي على آلاف النصوص، مما يصعّب العثور عليها وتجربة التعريب.

## الحل المقترح

### 1. فئة جديدة: "القائمة الرئيسية"
اضافة فئة `bdat-title-menu` مخصصة لنصوص شاشة العنوان والقوائم الأساسية، مع ايقونة وتصميم مميز يجعلها اول ما يراه المترجم.

### 2. تصنيف الجداول تلقائياً
الجداول التالية ستذهب للقائمة الرئيسية تلقائياً:
- `msg_mnu_common_ms` - الرسائل المشتركة للقوائم (تحتوي عادة New Game, Continue, Settings)
- `msg_mnu_title` - شاشة العنوان ان وجدت
- `msg_mnu_save` / `msg_mnu_load` - حفظ وتحميل
- `msg_mnu_option` - خيارات الاعدادات
- `msg_mnu_config` - اعدادات التهيئة

### 3. كشف ذكي بالمحتوى (المرحلة الاضافية)
بعد استخراج النصوص، يتم فحص النص الانجليزي الأصلي بحثاً عن كلمات مفتاحية لشاشة العنوان مثل:
- `New Game`, `Continue`, `Load`, `Save`, `Options`, `Settings`, `Quit`, `Title Screen`

اذا احتوى النص على هذه الكلمات ولم يكن مصنفاً بالفعل في فئة محددة، يُعلّم بعلامة خاصة تسهل البحث عنه.

---

## التفاصيل التقنية

### ملف `src/components/editor/types.tsx`
- اضافة `bdat-title-menu` الى مصفوفة `BDAT_CATEGORIES` مع ايقونة `Home` ولون مميز
- اضافة قواعد في `categorizeByTableName` لتوجيه `msg_mnu_common_ms` و `msg_mnu_title` و `msg_mnu_save` و `msg_mnu_option` الى `bdat-title-menu`

### ملف `src/hooks/useEditorState.ts`
- تحديث منطق التصنيف ليتعرف على الفئة الجديدة

### النتيجة المتوقعة
عند رفع ملفات BDAT، ستظهر فئة "القائمة الرئيسية" في اعلى القائمة مع ايقونة 🏠، تحتوي فقط على النصوص التي يراها اللاعب اول ما يفتح اللعبة - مما يسهل ترجمتها وتجربتها فوراً.

